<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MobileComputingProject" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:background">@color/wandermartSecondary</item>
    </style>

    <style name="cardView" parent="Base.Theme.MobileComputingProject">
        <item name="android:background">@color/wandermartAccent</item>

    </style>

    <style name="Button" parent="Base.Theme.MobileComputingProject">
        <item name="android:backgroundTint">@color/wandermartPrimary</item>
        <item name="android:radius">30dp</item>
    </style>

    <style name="Theme.MobileComputingProject" parent="Base.Theme.MobileComputingProject" />
</resources>